{
    "name": "typeselector-scrapper",
    "version": "1.0.0",
    "description": "TypeSelector's scrapper, it is used to fetch and format data from the google fonts main site.",
    "main": "index.js",
    "scripts": {
        "test": "mocha --timeout 10000",
        "start": "node index.js"
    },
    "author": "",
    "license": "ISC",
    "dependencies": {
        "chalk": "4.1.2",
        "dotenv": "^16.0.0",
        "puppeteer": "^13.1.3",
        "puppeteer-cluster": "^0.23.0"
    },
    "devDependencies": {
        "expect.js": "^0.3.1",
        "mocha": "^9.2.1"
    }
}
